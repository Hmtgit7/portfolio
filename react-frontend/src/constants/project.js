// All projects shown on the /projects page — uses real data from work.js
import works from './work';

// Export the same real dataset used in the Work section
const projects = works;

export default projects;
